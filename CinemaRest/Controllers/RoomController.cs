using System.Net;

namespace CinemaRest.Controllers
{
    public class RoomController : Controller
    {
        public RoomController(HttpListenerContext context) : base(context)
        {
        }
    }
}